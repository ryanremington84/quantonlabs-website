<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quanton Labs</title>

  <!-- Font -->
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --navy: #041227;
      --azure: #0080FF;
    }

    body {
      margin: 0;
      background: #ffffff;
      font-family: 'Manrope', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
    }

    header.site-header {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      padding: 20px 40px;
      background-color: #fff;
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 16px; /* space between icon and wordmark */
      text-decoration: none;
    }

    .brand .icon {
      width: 56px;  /* Adjust size to taste */
      height: 56px;
      display: block;
    }

    .brand .wordmark {
      display: flex;
      align-items: baseline;
    }

    .brand .wordmark span.q {
      font-size: 60px;
      font-weight: 600;
      letter-spacing: -0.02em;
      color: var(--navy);
    }

    .brand .wordmark span.labs {
      font-size: 60px;
      font-weight: 300;
      letter-spacing: 0.08em;
      color: var(--azure);
      margin-left: 10px;
    }

    .tagline {
      font-size: 18px;
      font-weight: 300;
      color: var(--azure);
      letter-spacing: 0.02em;
      margin-top: 4px;
    }

    .brand:hover {
      opacity: 0.9;
    }

    @media (prefers-reduced-motion: reduce) {
      .icon {
        content: url("/icon-static.svg"); /* fallback static icon */
      }
    }
  </style>
</head>

<body>
  <header class="site-header">
    <a href="/" class="brand" aria-label="Quanton Labs Home">
      <img src="/icon-animated.svg" alt="" class="icon" aria-hidden="true">

      <div class="wordmark">
        <span class="q">QUANTON</span>
        <span class="labs">LABS</span>
      </div>
    </a>
  </header>

  <!-- Optional tagline under header -->
  <div style="padding: 0 40px;">
    <p class="tagline">The Architecture of Intelligent Business</p>
  </div>
</body>
</html>
